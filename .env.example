# Browser: same-origin proxy by default. This value is embedded at build time.
NEXT_PUBLIC_API_URL=/backend-api

# Next.js server and proxy: evaluated at runtime.
API_INTERNAL_URL=http://127.0.0.1:8000/api
